{{/*
Copyright (C) 2025 Intel Corporation
SPDX-License-Identifier: Apache-2.0
*/}}

{{- $targetDevice := ternary "GPU" .Values.vlmServing.env.targetDevice .Values.vlmServing.gpu.enabled -}}
{{- $needsGpu := contains "GPU" $targetDevice -}}
{{- $weightFormat := default (ternary "int4" "int8" (or (contains "GPU" $targetDevice) (contains "NPU" $targetDevice))) .Values.vlmServing.env.weightFormat -}}
{{- $sanitized := .Values.vlmServing.env.modelName | replace "/" "_" | replace ":" "_" | replace " " "_" -}}
{{- $isOpenvino := hasPrefix "OpenVINO/" .Values.vlmServing.env.modelName -}}
{{- $storageName := ternary (printf "%s_%s" $sanitized $targetDevice) (printf "%s_%s_%s" $sanitized $targetDevice $weightFormat) $isOpenvino -}}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "stia.vlmServing.fullname" . }}
  labels:
    {{- include "stia.labels" . | nindent 4 }}
    app.kubernetes.io/component: vlm-serving
spec:
  replicas: {{ .Values.vlmServing.replicas }}
  selector:
    matchLabels:
      app: {{ include "stia.vlmServing.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "stia.vlmServing.fullname" . }}
        {{- include "stia.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: vlm-serving
    spec:
      {{- if $needsGpu }}
      securityContext:
        supplementalGroups:
          {{- toYaml .Values.vlmServing.gpu.renderGroupIds | nindent 10 }}
      {{- end }}
      {{- with .Values.vlmServing.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        - name: ovms-model-init
          image: "{{ .Values.vlmServing.initImage.repository }}:{{ .Values.vlmServing.initImage.tag }}"
          imagePullPolicy: {{ .Values.vlmServing.initImage.pullPolicy }}
          command:
            - /bin/sh
            - -c
            - |
              # Storage-aware model detection
              vlm_storage_name="{{ $storageName }}"
              vlm_model_path="/models/${vlm_storage_name}"

              config_has_model() {
                grep -q "\"name\": \"$1\"" /models/config.json 2>/dev/null
              }

              needs_init=false

              if [ -f /models/config.json ]; then
                echo "Config file found, checking for OVMS model."
                if [ ! -d "${vlm_model_path}" ] || [ ! -f "${vlm_model_path}/graph.pbtxt" ]; then
                  echo "Model folder ${vlm_storage_name} not found. Will export."
                  needs_init=true
                elif ! config_has_model "${vlm_storage_name}"; then
                  echo "Model ${vlm_storage_name} not in config. Will export."
                  needs_init=true
                fi
                if [ "$needs_init" = false ]; then
                  echo "Model ${vlm_storage_name} already exists with correct config."
                fi
              else
                echo "No config.json found, building OVMS model repository."
                needs_init=true
              fi

              if [ "$needs_init" = true ]; then
                . /config/ovms-init.sh
              fi
          args:
            - {{ .Values.vlmServing.env.modelName | quote }}
            - {{ $weightFormat | quote }}
            - {{ .Values.vlmServing.huggingfaceToken | quote }}
            - {{ $targetDevice | quote }}
          env:
            - name: http_proxy
              value: {{ .Values.global.proxy.httpProxy | quote }}
            - name: https_proxy
              value: {{ .Values.global.proxy.httpsProxy | quote }}
            - name: no_proxy
              value: "{{ .Values.global.proxy.noProxy }},.svc,.svc.cluster.local,.cluster.local"
            - name: PIP_TRUSTED_HOST
              value: "storage.openvinotoolkit.org download.pytorch.org"
          volumeMounts:
            - name: ov-models
              mountPath: /models
            - name: init-script
              mountPath: /config
              readOnly: true
      containers:
        - name: {{ .Values.vlmServing.name }}
          image: "{{- if $needsGpu -}}{{ .Values.vlmServing.image.repository }}:{{ .Values.vlmServing.image.gpuTag }}{{- else -}}{{ .Values.vlmServing.image.repository }}:{{ .Values.vlmServing.image.tag }}{{- end -}}"
          imagePullPolicy: {{ .Values.vlmServing.image.pullPolicy }}
          args:
            - "--port"
            - "9000"
            - "--rest_port"
            - "8000"
            - "--config_path"
            - "/models/config.json"
            - "--log_level"
            - {{ .Values.vlmServing.env.logLevel | quote }}
            - "--metrics_enable"
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            - name: no_proxy
              value: "{{ .Values.global.proxy.noProxy }},.svc,.svc.cluster.local,.cluster.local,{{ include "stia.vlmServing.fullname" . }},{{ include "stia.trafficAgent.fullname" . }}"
            - name: http_proxy
              value: {{ .Values.global.proxy.httpProxy | quote }}
            - name: https_proxy
              value: {{ .Values.global.proxy.httpsProxy | quote }}
          volumeMounts:
            - name: ov-models
              mountPath: /models
              readOnly: true
            {{- if $needsGpu }}
            - name: dri-device
              mountPath: /dev/dri
            {{- end }}
          livenessProbe:
            httpGet:
              path: /v2/health/live
              port: http
            initialDelaySeconds: {{ .Values.vlmServing.healthcheck.initialDelaySeconds }}
            periodSeconds: {{ .Values.vlmServing.healthcheck.periodSeconds }}
            timeoutSeconds: {{ .Values.vlmServing.healthcheck.timeoutSeconds }}
            failureThreshold: {{ .Values.vlmServing.healthcheck.failureThreshold }}
          readinessProbe:
            httpGet:
              path: /v2/health/ready
              port: http
            initialDelaySeconds: {{ .Values.vlmServing.healthcheck.initialDelaySeconds }}
            periodSeconds: {{ .Values.vlmServing.healthcheck.periodSeconds }}
            timeoutSeconds: {{ .Values.vlmServing.healthcheck.timeoutSeconds }}
            failureThreshold: {{ .Values.vlmServing.healthcheck.failureThreshold }}
          resources:
            {{- if $needsGpu }}
            {{- $res := deepCopy .Values.vlmServing.resources }}
            {{- $gpuLimits := dict .Values.vlmServing.gpu.resourceName (.Values.vlmServing.gpu.resourceLimit | int) }}
            {{- $_ := set $res "limits" (merge (default dict (get $res "limits")) $gpuLimits) }}
            {{- toYaml $res | nindent 12 }}
            {{- else }}
            {{- toYaml .Values.vlmServing.resources | nindent 12 }}
            {{- end }}
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: ov-models
          {{- if .Values.vlmServing.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "stia.vlmServing.fullname" . }}-models
          {{- else }}
          emptyDir: {}
          {{- end }}
        - name: init-script
          configMap:
            name: {{ include "stia.vlmServing.fullname" . }}-init-script
            defaultMode: 0755
        {{- if $needsGpu }}
        - name: dri-device
          hostPath:
            path: /dev/dri
            type: Directory
        {{- end }}
